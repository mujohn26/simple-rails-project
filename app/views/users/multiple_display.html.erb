<div>
<h3>Do you want to delete the following users ?</h3>
<%@users.each do |user| %>
  <div>
  <%=user.name%>
  </div>
<% end %>
<br>
  <%= link_to "Yes", delete_multiple_users_path(@users), method: :delete, params: @params[:ids]%>
  <%= link_to 'Back', users_path %>
</div>